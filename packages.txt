how to install packages
pip install openpyxl 
it used for excel file use 
pylint: this is a library that looks at your code and reports back possible issues.(pip install pylint)
unittest:This is a built-in library will allow to test your own programs and check you are getting desired outputs.